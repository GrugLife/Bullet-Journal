<% include ../partials/header %>

<div class="container">
    <div class="row">
        <h1 style="text-align: center">Edit Task</h1>
        <div style="width: 30%; margin: 25px auto;">
            <form action="/bullet/<%= bullet_id %>/tasks/<%= task._id %>?_method=PUT" method="POST">
                <div class="form-group">
                    <input class="form-control" type="text" value="<%= task.task %>" name="task[task]" placeholder="task">
                </div>
                <!--<div class="checkbox">-->
                <!--   <label>-->
                <!--     <form action="/bullet/<%= bullet_id %>/tasks/<%= task._id %>?_method=PUT" method="POST">-->
                <!--         <input type="checkbox" class="strikethrough" value="<%= task.completed %>" onchange="doalert(this)" name="task[completed]" placeholder="task"><span><%= task.task %></span>-->
                <!--     </form>-->
                <!--   </label>-->
                <!--</div>-->
              <div class="form-group">
                    <button class="btn btn-lg btn-primary btn-block">Submit</button>
                </div>
            </form>
            <a href="/bullet">Go Back</a>
        </div>
    </div>
</div>
<script>
    function doalert(checkboxElem) {
        console.log("hello worlds")
      if (checkboxElem.checked) {
          db.tasks.updateOne(
              { task: this },
              {
                  $set: {completed: true}
              }
        )
    } else {
          db.tasks.updateOne(
              { task: this },
              {
                  $set: {completed: false}
              }
        )
      }
    }
</script>
           

<% include ../partials/footer %>